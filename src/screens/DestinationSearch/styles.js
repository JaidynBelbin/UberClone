import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
    height: '100%',
  },
  placesAutocomplete: {
    padding: 15,
    backgroundColor: '#eee',
    marginVertical: 5,
  },
});

export default styles;
